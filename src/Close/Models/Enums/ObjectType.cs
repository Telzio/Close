namespace Close.Models.Enums;

public enum ObjectType
{
    lead,
    emailthread,
    call,
    opportunity,
    sms,
    taskcompleted,
    custom_object,
    contact,
    contact_email,
    contact_phone
}