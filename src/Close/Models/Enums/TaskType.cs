namespace Close.Models.Enums;

public enum TaskType
{
    lead,
    incoming_email,
    email_followup,
    missed_call,
    answered_detached_call,
    voicemail,
    opportunity_due,
    incoming_sms
}